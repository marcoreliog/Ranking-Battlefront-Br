import Link from "next/link";import {createClient} from "@/lib/supabase/server";import {Card} from "@/components/ui/card";
export default async function HeroesPage(){const supabase=await createClient();const {data:heroes}=await supabase.from("public_heroes").select("*").order("side").order("name");return <section><p className="text-xs font-bold tracking-[.18em] text-cyan-300">RANKINGS POR PERSONAGEM</p><h1 className="mt-1 text-3xl font-black">Heróis e vilões</h1><p className="mt-2 text-slate-400">Escolha um personagem para ver o desempenho médio de cada jogador.</p><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{heroes?.map(hero=><Link key={hero.slug} href={`/herois/${hero.slug}`}><Card className={hero.side==="light"?"hover:border-cyan-400":"hover:border-rose-400"}><span className={hero.side==="light"?"text-cyan-300":"text-rose-300"}>{hero.side==="light"?"Lado da luz":"Lado sombrio"}</span><h2 className="mt-1 text-lg font-bold">{hero.name}</h2></Card></Link>)}</div></section>}
