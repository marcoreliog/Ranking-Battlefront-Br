import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {resolve} from "node:path";
const sql=readFileSync(resolve(process.cwd(),"supabase/migrations/20260920120000_initial_schema.sql"),"utf8");
describe("contrato do banco",()=>{it("impede notas duplicadas e atualiza a nota existente de forma concorrente",()=>{expect(sql).toContain("unique (user_id, player_id, mode)");expect(sql).toContain("on conflict (user_id, player_id, mode) do update");});it("impede Top 4 repetido e exige posições válidas",()=>{expect(sql).toContain("unique (profile_id, hero_id)");expect(sql).toContain("position between 1 and 4");expect(sql).toContain("choose exactly four different heroes");});it("protege administração via RLS e função segura",()=>{expect(sql).toContain("create or replace function public.is_admin()");expect(sql).toContain("players: admins manage");expect(sql).toContain("role changes are only allowed");});});
