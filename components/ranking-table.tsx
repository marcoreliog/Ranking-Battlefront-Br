import Link from "next/link"; import type { RankingRow } from "@/lib/data";
export function RankingTable({ rows, detailed = false }: { rows: RankingRow[]; detailed?: boolean }) { if (!rows.length) return <div className="rounded-xl border border-dashed border-slate-700 p-8 text-center text-slate-400">Ainda não há avaliações suficientes. Seja o primeiro a contribuir.</div>; return <div className="overflow-x-auto rounded-xl border border-slate-800"><table className="w-full min-w-[640px] text-left text-sm"><thead className="bg-slate-900 text-xs uppercase tracking-wider text-slate-400"><tr><th className="p-4">Pos.</th><th className="p-4">Jogador</th>{detailed && <><th className="p-4">Assalto</th><th className="p-4">HvV</th><th className="p-4">Confronto</th></>}<th className="p-4">Média</th><th className="p-4">Avaliações</th><th className="p-4">Status</th></tr></thead><tbody>{rows.map((row) => <tr key={row.player_slug} className="border-t border-slate-800 hover:bg-slate-900/60"><td className="p-4 font-bold text-cyan-300">{row.position ? `#${row.position}` : "—"}</td><td className="p-4"><Link className="font-semibold hover:text-cyan-300" href={`/jogadores/${row.player_slug}`}>{row.display_name || row.gamertag}</Link><span className="ml-2 text-slate-500">{row.gamertag}</span></td>{detailed && <><td className="p-4">{row.assault_average?.toFixed(2)}</td><td className="p-4">{row.hvv_average?.toFixed(2)}</td><td className="p-4">{row.showdown_average?.toFixed(2)}</td></>}<td className="p-4 text-lg font-bold">{Number(row.average_score).toFixed(2)}</td><td className="p-4">{row.ratings_count}</td><td className="p-4">{row.provisional ? <span className="rounded bg-amber-400/15 px-2 py-1 text-xs text-amber-200">Provisória</span> : <span className="rounded bg-cyan-400/15 px-2 py-1 text-xs text-cyan-200">Oficial</span>}</td></tr>)}</tbody></table></div>; }
